# Server
PORT=4000
NODE_ENV=development

# Same Postgres database as the Next.js app (@ and ! in password are URL-encoded)
DATABASE_URL="postgresql://postgres:Letmein%4099x%21@localhost:5432/tailorsend"

# Must match NEXT_PUBLIC_API_URL / frontend origin for CORS
FRONTEND_URL="http://localhost:3000"

# Session signing secret — use a long random string in production
SESSION_SECRET="cN0j0WKIlFLUAIBl0+Qr2/PIo9FFYgkmeAgrC9zz1MA="

# Password reset links point here (your Netlify URL in production)
APP_URL="http://localhost:3000"

# Optional SMTP for forgot-password emails (omit to log reset links in dev)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER=""
# SMTP_PASS=""
# SMTP_FROM="AutoApply <noreply@yourdomain.com>"
